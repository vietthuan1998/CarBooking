# TODO

## Router & structure refactor
- [x] Inspect existing routing setup (App/main) and confirm current route strategy (was missing).
- [x] Design new routes layout: `/admin/*` using `AdminLayout` + sidebar; keep public/user routes separate.

- [ ] Create route components/pages placeholders if missing.
- [ ] Refactor `Sidebar` to render inside a layout route and use active link styling.
- [ ] Update `App.tsx` to use `react-router-dom` (or current router) with nested routes.
- [ ] Ensure build passes and navigation works.

## Testing
- [ ] Run `npm test`/`npm run build` (as available).

